{
  "name": "cricalert",
  "private": true,
  "scripts": {
    "dev": "sh -c 'if [ -d web ]; then cd web; fi && npm run dev'",
    "build": "sh -c 'if [ -d web ]; then cd web; fi && npm ci && npm run build'",
    "start": "sh -c 'if [ -d web ]; then cd web; fi && npm run start'",
    "lint": "sh -c 'if [ -d web ]; then cd web; fi && npm run lint'"
  }
}
