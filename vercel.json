{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "sh -c 'if [ -d web ]; then cd web; fi && npm ci'",
  "buildCommand": "sh -c 'if [ -d web ]; then cd web; fi && npm run build'"
}
